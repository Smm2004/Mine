
spring.application.name=Course_Registration

# Common mail settings (can be overridden by profiles if needed)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=marghalani.siraj@gmail.com
spring.mail.password=baub jqac mqwt xhka
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# JWT Configuration (common across profiles, but secret might differ in prod)
jwt.secret=YourSuperSecretKeyThatIsAtLeast256BitsLongAndShouldBeRandomlyGeneratedInProduction
jwt.expiration=86400000

# Liquibase Configuration (common)
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
spring.liquibase.enabled=true

# Hibernate Identifier Quoting Strategy (common)
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.globally_quoted_identifiers_skip_column_definitions=true
spring.jpa.open-in-view=false

# Default logging level (can be overridden by profiles)
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.com.smm.course_registration=DEBUG

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springdoc=DEBUG

spring.profiles.active=local